import { a as frappeCall, r as camelizeKeys } from "./http-BM0VI1yy.js";
//#region src/services/moderation.service.ts
function mapCase(raw) {
	const data = camelizeKeys(raw);
	const rawCategory = String(data["category"] ?? "").toLowerCase();
	const rawRequestedBy = String(data["requestedBy"] ?? "").toLowerCase();
	return {
		id: String(data["name"] ?? ""),
		projectId: String(data["project"] ?? ""),
		projectTitle: String(data["projectTitle"] ?? ""),
		clientName: String(data["clientName"] ?? ""),
		agencyName: data["agencyName"] ?? null,
		category: rawCategory.includes("litige") ? "dispute" : "amicable",
		requestedBy: rawRequestedBy === "client" || rawRequestedBy === "agency" || rawRequestedBy === "system" ? rawRequestedBy : null,
		justification: String(data["justification"] ?? ""),
		createdAt: String(data["creation"] ?? "")
	};
}
async function listPendingSuspensions() {
	const raw = await frappeCall("moderation.list_pending_suspensions", {});
	return Array.isArray(raw) ? raw.map(mapCase) : [];
}
async function approveSuspensionAsModerator(id) {
	await frappeCall("moderation.approve_suspension", { suspension: id });
}
async function refuseSuspensionAsModerator(id) {
	await frappeCall("moderation.refuse_suspension", { suspension: id });
}
async function resolveDispute(id, founded, decisionNote) {
	const raw = await frappeCall("moderation.resolve_dispute", {
		dispute: id,
		founded,
		decision_note: decisionNote
	});
	const data = camelizeKeys(raw);
	return {
		id: String(data["name"] ?? id),
		status: String(data["status"] ?? (founded ? "Founded" : "Not Founded"))
	};
}
function mapLitigeNoticeCase(raw) {
	const data = camelizeKeys(raw);
	const rawNoticeStatus = data["litigeNoticeStatus"];
	return {
		...mapCase(raw),
		litigeNoticeStatus: rawNoticeStatus === "Pending" || rawNoticeStatus === "Responded" ? rawNoticeStatus : null,
		agencyNoticeDeadline: data["agencyNoticeDeadline"] ?? null,
		agencyResponse: data["agencyResponse"] ?? null,
		agencyResponseDate: data["agencyResponseDate"] ?? null
	};
}
async function listPendingLitigeNotices() {
	const raw = await frappeCall("moderation.list_pending_litige_notices", {});
	return Array.isArray(raw) ? raw.map(mapLitigeNoticeCase) : [];
}
async function resolveLitigeNotice(id, acceptAgencyJustification, decisionNote) {
	await frappeCall("moderation.resolve_litige_notice", {
		suspension: id,
		accept_agency_justification: acceptAgencyJustification,
		decision_note: decisionNote
	});
}
function mapPendingAgencyReview(raw) {
	const data = camelizeKeys(raw);
	return {
		id: String(data["name"] ?? ""),
		clientProfile: data["clientProfile"] ?? null,
		clientName: String(data["clientName"] ?? ""),
		agency: String(data["agency"] ?? ""),
		agencyName: data["agencyName"] ?? null,
		project: String(data["project"] ?? ""),
		rating: Number(data["rating"] ?? 0),
		comment: String(data["comment"] ?? ""),
		createdAt: String(data["creation"] ?? "")
	};
}
async function listPendingAgencyReviews() {
	const raw = await frappeCall("moderation.list_pending_reviews", {});
	return Array.isArray(raw) ? raw.map(mapPendingAgencyReview) : [];
}
async function moderateAgencyReview(id, approve) {
	await frappeCall("moderation.moderate_review", {
		review: id,
		approve: approve ? 1 : 0
	});
}
function mapRecentClientReview(raw) {
	const data = camelizeKeys(raw);
	return {
		id: String(data["name"] ?? ""),
		client: String(data["client"] ?? ""),
		clientName: String(data["clientName"] ?? ""),
		agency: String(data["agency"] ?? ""),
		agencyName: data["agencyName"] ?? null,
		project: String(data["project"] ?? ""),
		rating: Number(data["rating"] ?? 0),
		comment: String(data["comment"] ?? ""),
		createdAt: String(data["creation"] ?? "")
	};
}
async function listRecentClientReviews() {
	const raw = await frappeCall("moderation.list_recent_client_reviews", {});
	return Array.isArray(raw) ? raw.map(mapRecentClientReview) : [];
}
async function suspendAccount(accountType, target, reason) {
	await frappeCall("moderation.suspend_account", {
		account_type: accountType,
		target,
		reason
	});
}
async function flagAccount(accountType, target, reason) {
	await frappeCall("moderation.flag_account", {
		account_type: accountType,
		target,
		reason
	});
}
//#endregion
export { listPendingSuspensions as a, refuseSuspensionAsModerator as c, suspendAccount as d, listPendingLitigeNotices as i, resolveDispute as l, flagAccount as n, listRecentClientReviews as o, listPendingAgencyReviews as r, moderateAgencyReview as s, approveSuspensionAsModerator as t, resolveLitigeNotice as u };
