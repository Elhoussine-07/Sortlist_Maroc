import { t as DashboardShell } from "./DashboardShell-t2TYp7B0.js";
import { t as EmptyState } from "./EmptyState-CjCsYQbe.js";
import { c as StatusTabs, i as SectionCard, r as SearchInput, s as StatusBadge } from "./Blocks-CStVFDlw.js";
import { n as StackSkeleton } from "./Skeletons-COgUvsAH.js";
import { n as ListPagination } from "./ListControls-FMqnx6XI.js";
import { t as DataTable } from "./DataTable-EspjDfBC.js";
import { r as getOpportunities } from "./opportunities.service-CTW7kUZZ.js";
import { useState } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
import { Workflow } from "lucide-react";
//#region src/services/workflow.service.ts
var STEP_LABELS = {
	received: "Reçues",
	reviewed: "Étudiées",
	quote_sent: "Devis envoyé",
	awaiting_client: "En attente client",
	won: "Gagnées",
	lost: "Perdues"
};
function deriveStep(rawStatus) {
	switch (rawStatus) {
		case "Reçue": return "received";
		case "Acceptée": return "reviewed";
		case "Devis envoyé": return "quote_sent";
		case "Gagnée": return "won";
		case "Archivée": return "lost";
		default: return "received";
	}
}
async function fetchWorkflowOpportunities() {
	const [offers, won, archived] = await Promise.all([
		getOpportunities({
			tab: "offers",
			page: 1,
			pageSize: 100
		}),
		getOpportunities({
			tab: "won",
			page: 1,
			pageSize: 100
		}),
		getOpportunities({
			tab: "archived",
			page: 1,
			pageSize: 100
		})
	]);
	return [
		...offers.items,
		...won.items,
		...archived.items
	];
}
async function getWorkflowStages() {
	const items = await fetchWorkflowOpportunities();
	const counts = {
		received: 0,
		reviewed: 0,
		quote_sent: 0,
		awaiting_client: 0,
		won: 0,
		lost: 0
	};
	for (const item of items) {
		const step = deriveStep(item.rawStatus);
		counts[step] += 1;
		if (step === "quote_sent") counts.awaiting_client += 1;
	}
	return Object.keys(STEP_LABELS).map((step) => ({
		id: step,
		step,
		label: STEP_LABELS[step],
		count: counts[step],
		slaHours: null
	}));
}
async function getWorkflowItems(params) {
	const page = params?.page ?? 1;
	const pageSize = params?.pageSize ?? 20;
	const allItems = await fetchWorkflowOpportunities();
	const counts = { all: allItems.length };
	for (const step of Object.keys(STEP_LABELS)) counts[step] = 0;
	for (const item of allItems) {
		const step = deriveStep(item.rawStatus);
		counts[step] = (counts[step] ?? 0) + 1;
		if (step === "quote_sent") counts["awaiting_client"] = (counts["awaiting_client"] ?? 0) + 1;
	}
	const filtered = params?.step ? allItems.filter((item) => {
		const step = deriveStep(item.rawStatus);
		if (params.step === "awaiting_client") return step === "quote_sent";
		return step === params.step;
	}) : allItems;
	const sorted = params?.sort === "deadline" ? [...filtered].sort((a, b) => (a.remainingHours ?? Infinity) - (b.remainingHours ?? Infinity)) : [...filtered].sort((a, b) => b.publishedAt.localeCompare(a.publishedAt));
	const total = sorted.length;
	const totalPages = Math.max(1, Math.ceil(total / pageSize));
	const start = (page - 1) * pageSize;
	return {
		items: sorted.slice(start, start + pageSize),
		total,
		page,
		pageSize,
		totalPages,
		counts
	};
}
//#endregion
//#region src/routes/_authenticated/agence.workflow.tsx?tsr-split=component
var STEP_TABS = [
	{
		value: "all",
		label: "Toutes"
	},
	{
		value: "quote_sent",
		label: "Devis"
	},
	{
		value: "awaiting_client",
		label: "Négociation"
	},
	{
		value: "won",
		label: "Signature"
	}
];
var COLUMNS = [
	{
		key: "item",
		header: "Opportunité",
		width: "minmax(0,2.2fr)",
		render: (item) => /* @__PURE__ */ jsxs("div", {
			className: "min-w-0",
			children: [/* @__PURE__ */ jsx("p", {
				className: "truncate text-[13.5px] font-bold",
				children: item.projectTitle
			}), /* @__PURE__ */ jsx("p", {
				className: "truncate text-[13px] text-muted-foreground",
				children: item.companyName
			})]
		})
	},
	{
		key: "stage",
		header: "Étape",
		render: (item) => /* @__PURE__ */ jsx(StatusBadge, { label: item.stepLabel })
	},
	{
		key: "quote",
		header: "Devis",
		render: (item) => /* @__PURE__ */ jsx("p", {
			className: "truncate text-[13px]",
			children: item.quoteAmount === null ? "—" : String(item.quoteAmount)
		})
	},
	{
		key: "remaining",
		header: "Temps restant",
		render: (item) => /* @__PURE__ */ jsx("p", {
			className: "truncate text-[13px] text-muted-foreground",
			children: item.remainingHours === null ? "—" : `${item.remainingHours} h`
		})
	}
];
function AgencyWorkflowPage() {
	const stagesQuery = useQuery({
		queryKey: [
			"agency",
			"workflow",
			"stages"
		],
		queryFn: getWorkflowStages
	});
	const stages = stagesQuery.data ?? [];
	const isStagesLoading = stagesQuery.isLoading;
	const [query, setQuery] = useState("");
	const [activeStage, setActiveStage] = useState("all");
	const itemsQuery = useQuery({
		queryKey: [
			"agency",
			"workflow",
			"items",
			activeStage
		],
		queryFn: () => getWorkflowItems(activeStage === "all" ? {} : { step: activeStage })
	});
	const allItems = itemsQuery.data?.items ?? [];
	const isItemsLoading = itemsQuery.isLoading;
	const counts = itemsQuery.data?.counts ?? {};
	const page = itemsQuery.data?.page ?? 1;
	const totalPages = itemsQuery.data?.totalPages ?? null;
	const items = query.trim() ? allItems.filter((item) => `${item.projectTitle} ${item.companyName}`.toLowerCase().includes(query.trim().toLowerCase())) : allItems;
	return /* @__PURE__ */ jsx(DashboardShell, {
		role: "agency",
		children: /* @__PURE__ */ jsxs("div", {
			className: "mx-auto max-w-[1080px]",
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: "flex items-start gap-3",
					children: [/* @__PURE__ */ jsx(Workflow, {
						className: "mt-1 h-[22px] w-[22px] shrink-0",
						strokeWidth: 1.6
					}), /* @__PURE__ */ jsxs("div", {
						className: "min-w-0",
						children: [/* @__PURE__ */ jsx("h1", {
							className: "text-[24px] font-bold tracking-tight",
							children: "Workflow"
						}), /* @__PURE__ */ jsx("p", {
							className: "mt-1 text-[14px] text-muted-foreground",
							children: "Étapes de traitement de vos opportunités."
						})]
					})]
				}),
				/* @__PURE__ */ jsx("section", {
					className: "mt-7",
					children: /* @__PURE__ */ jsx(SectionCard, {
						title: "Étapes du workflow",
						description: "Répartition de vos opportunités par étape.",
						children: isStagesLoading ? /* @__PURE__ */ jsx(StackSkeleton, { count: 4 }) : stages.length === 0 ? /* @__PURE__ */ jsx(EmptyState, { message: "Aucune donnée disponible" }) : /* @__PURE__ */ jsx("ol", {
							className: "grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4",
							children: stages.map((stage) => /* @__PURE__ */ jsxs("li", {
								className: "rounded-lg border border-border p-4",
								children: [/* @__PURE__ */ jsx("p", {
									className: "text-[13px] text-muted-foreground",
									children: stage.label
								}), /* @__PURE__ */ jsx("p", {
									className: "mt-1 text-[26px] font-bold leading-none",
									children: stage.count
								})]
							}, stage.id))
						})
					})
				}),
				/* @__PURE__ */ jsx("div", {
					className: "mt-7",
					children: /* @__PURE__ */ jsx(SearchInput, {
						value: query,
						onChange: setQuery,
						placeholder: "Rechercher une opportunité..."
					})
				}),
				/* @__PURE__ */ jsx("div", {
					className: "mt-6",
					children: /* @__PURE__ */ jsx(StatusTabs, {
						tabs: STEP_TABS,
						value: activeStage,
						onChange: setActiveStage,
						counts
					})
				}),
				/* @__PURE__ */ jsx("div", {
					className: "mt-6",
					children: /* @__PURE__ */ jsx(DataTable, {
						columns: COLUMNS,
						rows: items,
						isLoading: isItemsLoading
					})
				}),
				/* @__PURE__ */ jsx(ListPagination, {
					page,
					totalPages
				}),
				/* @__PURE__ */ jsxs("p", {
					className: "mt-6 text-[13px] text-muted-foreground",
					children: [
						"Pour accepter une opportunité ou envoyer un devis, rendez-vous sur l'écran",
						" ",
						/* @__PURE__ */ jsx(Link, {
							to: "/agence/opportunites",
							className: "font-semibold underline underline-offset-2",
							children: "Opportunités"
						}),
						"."
					]
				})
			]
		})
	});
}
//#endregion
export { AgencyWorkflowPage as component };
