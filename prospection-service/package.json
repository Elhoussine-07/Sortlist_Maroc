{
  "name": "prospection-service",
  "version": "1.0.0",
  "description": "Prospection augmentée (module 2.6) — détection d'entreprises par IP, score d'intention d'achat, emails IA, campagnes. Cf. docs/INTEGRATION.md §6.",
  "private": true,
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "migrate": "node src/models/migrate.js",
    "check": "for f in src/*.js src/**/*.js; do node --check \"$f\" || exit 1; done"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
